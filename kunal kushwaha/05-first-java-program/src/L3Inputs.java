import java.util.Scanner;
public class L3Inputs {
    public static void main(String[] args) {

        //taking input in java
        Scanner sc = new Scanner(System.in);
        System.out.print("please enter a number : ");
        int rollno = sc.nextInt();
        System.out.println("Your roll number is : " + rollno);


//        String name = sc.next(); // take a word as input
//        String sen = sc.nextLine(); // takes whole line as input
//        float num = sc.nextFloat(); // if int is passed in input, it will be converted to flaot, because float is greater than int.
    }
}
